{
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "start": "web-dev-server --node-resolve --open --watch",
        "test": "web-test-runner \"out/test/**/*.test.js\" --node-resolve",
        "test:cover": "web-test-runner out/test/**/*.test.js --node-resolve --coverage"
    },
    "dependencies": {
        "@open-wc/testing": "^3.1.4",
        "lit": "^2.2.2",
        "tslib": "^2.4.0"
    },
    "devDependencies": {
        "@esm-bundle/chai": "^4.3.4-fix.0",
        "@web/dev-server": "^0.1.31",
        "@web/test-runner": "^0.13.27"
    }
}
